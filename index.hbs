{{!< default}}

<div class="col-md-9 col-md-offset-3">

            <div class="grid">
            {{!-- Iterate through each posts --}}
            <div class="grid-sizer"></div>
            {{#foreach posts}}
                {{!-- Get post card partial --}}
                {{> post-card}}

            {{/foreach}}
            </div>

            {{pagination}}
        
</div>
</div>
</div>
