{{!--
*   MARIE Ghost Theme
*   https://macn.tech
*   Thank you for using this theme!
*   Follow me on Github @johannesgeisslinger
*--}}


{{!-- Mobiles Menu  --}}
<div class="mobile">
    <div class="container">

      <div class="menu-toggler"><i class="fas fa-bars"></i></div>
        <div class="logo">
                        {{!-- Logo check  --}}
                        {{#if @site.logo}}
                            <img src="{{@site.logo}}" alt="{{@site.title}}" width="85">
                        {{else}}
                            <span>{{@site.title}}</span>
                        {{/if}}

        </div>

        <div class="description-mobil">                          
                {{@site.description}}
        </div>

    </div>
</div>


<div class="hide-menu"></div>
{{!-- END Mobil Menu  --}}

<div class="container">
    <div class="row">
        <div class="col-lg-3">
            <div class="header">                
                        {{!-- Disable for mobile view  --}}
                        <div class="logo logo-mobile">                                               
                             
                            {{#if @site.logo}}
                                <img src="{{@site.logo}}" alt="{{@site.title}}" width="150">
                            {{else}}
                                <span>{{@site.title}}</span>
                            {{/if}}

                        </div>
                     
                        
                        <div class="description description-screen">
                            
                            {{@site.description}}
                            
                        </div>
                        
                        {{!-- Navigation  --}}
                        {{"navigation"}}
                        

                        {{!--  Social Links via Secondary Navigation --}}
                        <div class="socials">                      
                            {{navigation type="secondary"}} 
                        </div>                  
                        


                        {{!-- Font Controller  --}}
                        <div class="fontcontroller">
                            <p>
                                <a class="increase">A+</a>
                                <a class="decrease">A-</a>
                                <a class="reset">R</a>
                            </p>
                        </div>    

                        {{!-- Theme Switch --}}
                        <div class="mb-3">  
                                <label class="switch">
                                    <input id="theme-switch" type="checkbox">
                                    <span class="slider">
                                        <i class="fas fa-sun"></i>
                                        <i class="fas fa-moon"></i>
                                    </span>
                                </label>
                        </div> 

                        {{!-- KOFI Button  --}}
                        <div class="mb-3" id="kofi"></div>
                        <script>
                            if(kofi) {
                                let content ='<a href="'+ kofiurl +'" target="_blank"><button type="button" class="btn btn-dark">Donate a <i class="fas fa-coffee"></i></button></a>';
                                document.getElementById('kofi').insertAdjacentHTML('afterbegin', content); 
                            }                                           
                        </script>

                        {{!-- Table of Contents if page = post // Including sticky.js to make TOC sticky if desktop mode--}}
                        {{#post}}
                            <hr >
                            <div class="toc sticky" data-margin-top="40" data-sticky-for="991"></div>
                        {{/post}}
                        {{!-- End TOC  --}}

            </div>
        </div>

  